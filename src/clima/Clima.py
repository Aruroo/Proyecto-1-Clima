class Clima ():

    """
        Función que se encarga de iniciar el proceso de consulta de clima con
        base en el archivo .csv que es pasado como parámetro.
    """
    def function1():
        c1 = Ciudad()
        c2 = Ciudad()

    """
        Función que se encarga de procesar la petición o bien, arrojar datos de
        la ciudad dada por el usuario en la función 1.
    """
    def function2():
        """
        revisar en caché
        """

        """
        Bandera para saber si la información solicitada se encuentra en caché.
        True si se encuentra la información en caché.
        False si no se encuentra la información en caché.
        """
        b = true

        if b:
            """
            Devuelve el archivo .json que contiene los datos a recolectar necesarios.
            """
            pass
        else:
            """
            Realizar petición para devolver un archivo .json.
            """

    """
        Función que se encarga de recolectar los datos necesarios del archivo
        .json dado por la función anterior.
    """
    def function3():
        return
